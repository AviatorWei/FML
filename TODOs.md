# CICD需求

1. 

# 功能性需求

## 暗标（Auction）

1. 引入条件解约（世界杯可以不需要）
   1. 条件解约：指在一个暗标中，玩家可以使用负数标记某个已有球员。若有球员中标，则按从大到小的顺序解约对应球员，即有一个中标则解约rank为-1的球员，以此类推。若暗标无球员中标，则不解约。
   2. 暗标有效性改为：已有球员的数量 + 被暗标的数量 - 条件解约的数量 <= 球员大名单数量上限
   3. 该方案需要纳入暗标的可配置化项
2. 可以考虑在初始化rank的时候，如果没有rank则使用100
3. 如果出现了相同rank的情况，将出价高的rank设为100. 不检查rank > 30的重复性
4. 

# 可读性需求

## Configuration

1. 帮我将所有的"fme_goal"替换为"goal"或者"valid_goal"，并且避免出现enum使用复数的情况，比方说你在yaml config中就使用了"fme_goals"，这对于维护代码不太方便